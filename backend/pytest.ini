[pytest]
# Solo recogemos tests de la carpeta tests/ para no confundir routers/progreso_test.py
# (que NO es un test, pero coincidiría con el patrón por defecto *_test.py).
testpaths = tests
python_files = test_*.py
